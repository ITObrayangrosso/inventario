{% extends 'base.html.twig' %}

{% block body %}
    <h1>Procesador</h1>

    <table>
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ procesador.descripcion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ procesador.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('procesador_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('procesador_edit', { 'id': procesador.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
