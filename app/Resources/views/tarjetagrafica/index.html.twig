{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lista de Tarjetas graficas</h1>

    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tarjetaGrafica in tarjetaGraficas %}
            <tr>
                <td><a href="{{ path('tarjetagrafica_show', { 'id': tarjetaGrafica.id }) }}">{{ tarjetaGrafica.descripcion }}</a></td>
                <td>
                    <ul>
                        <li class="btn boton_crear">
                            <a href="{{ path('tarjetagrafica_show', { 'id': tarjetaGrafica.id }) }}">Ver</a>
                        </li>
                        <li class="btn boton_crear">
                            <a href="{{ path('tarjetagrafica_edit', { 'id': tarjetaGrafica.id }) }}">Editar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a class="btn boton_crear" href="{{ path('tarjetagrafica_new') }}">Crear Nuevo</a>
{% endblock %}
