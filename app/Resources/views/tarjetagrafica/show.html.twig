{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tarjeta grafica</h1>

    <table class="table table-striped table-bordered table-hover">
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ tarjetaGrafica.descripcion }}</td>
            </tr>
        </tbody>
    </table>
    <a class="btn boton_crear" href="{{ path('tarjetagrafica_edit', { 'id': tarjetaGrafica.id }) }}">Editar</a>
    <a href="{{ path('tarjetagrafica_index') }}">Volver a la lista</a>
{% endblock %}
