{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ubicacion creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        </br><input type="submit"  class="btn boton_crear " value="Guardar" />
    {{ form_end(form) }}
    <br>
    <a href="{{ path('ubicacion_index') }}">Volver a la lista</a>

{% endblock %}
