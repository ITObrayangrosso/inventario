{% extends 'base.html.twig' %}

{% block body %}
    <h1>Monitor creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        </br><input type="submit"  class="btn boton_crear " value="Guardar" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('monitor_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
