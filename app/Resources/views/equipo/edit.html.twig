{% extends 'base.html.twig' %}

{% block body %}
    <h1>Equipo edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <br><input type="submit" value="Guardar" class="btn boton_crear" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('equipo_index') }}">Volver a la lista</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
