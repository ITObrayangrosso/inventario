{% extends 'base.html.twig' %}

{% block body %}
    <h1>Empleado edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}</br>
        <input type="submit" class="btn boton_crear" value="Guardar" />
    {{ form_end(edit_form) }}
    </br>
    <a href="{{ path('empleado_index') }}">Volver a la lista</a>
{% endblock %}
