{% extends 'base.html.twig' %}

{% block body %}
    <h1>Editar Área</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}</br>
        <input type="submit" class="btn boton_crear" value="Guardar" />
    {{ form_end(edit_form) }}
    </br>
    <ul>
        <li>
            <a href="{{ path('area_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
