{% extends 'base.html.twig' %}

{% block body %}
    <h1>Area</h1>

    <table class="table table-striped table-bordered table-hover">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ area.nombre }}</td>
            </tr>
        </tbody>
    </table>
    <a  class="btn boton_crear" href="{{ path('area_edit', { 'id': area.id }) }}">Editar</a>
    <a href="{{ path('area_index') }}">Volver a la lista</a>
{% endblock %}
