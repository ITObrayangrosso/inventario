{% extends 'base.html.twig' %}

{% block body %}
    <h1>Memoria ram</h1>

    <table class="table table-striped table-bordered table-hover">
        <tbody>
            <tr>
                <th>Descripcion</th>
                <td>{{ memoriaRam.descripcion }}</td>
            </tr>
        </tbody>
    </table>
    <a  class="btn boton_crear" href="{{ path('memoriaram_edit', { 'id': memoriaRam.id }) }}">Editar</a>
    <a href="{{ path('memoriaram_index') }}">Volver a la lista</a>
{% endblock %}
